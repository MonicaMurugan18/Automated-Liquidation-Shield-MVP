# Pinned against Python 3.14. pydantic must be >= 2.12 for 3.14 wheels.
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
python-dotenv==1.2.3

# Optional: only imported when SUPABASE_URL / SUPABASE_SERVICE_KEY are set.
# The app runs without it, falling back to in-memory persistence.
supabase==2.31.0

# development
pytest==9.1.1
httpx==0.28.1
